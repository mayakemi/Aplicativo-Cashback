PORT=4000
MONGODB_URI=mongodb://localhost:27017/clinic_agendamentos
JWT_SECRET=troque-por-uma-chave-forte
JWT_EXPIRES_IN=1d

# Invite code para permitir cadastro de secretários (pacientes ficam liberados por padrão)
SECRETARY_INVITE_CODE=SECRETARIO123

# Chave da API do clima (OpenWeatherMap). Se não estiver definida, o agendamento funciona sem alerta de chuva.
OPENWEATHERMAP_API_KEY=
OPENWEATHERMAP_UNITS=metric

